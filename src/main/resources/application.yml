spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/mini
    username: root
    password: mymysql
    hikari:
      maximum-pool-size: 10
      connection-timeout: 5000
      connection-init-sql: SELECT 1
      validation-timeout: 2000
      minimum-idle: 10
      idle-timeout: 600000
      max-lifetime: 1800000
#maximum-pool-size: ?? pool size (defailt 10)
#connection-timeout: (? ???)
#connection-init-sql: SELECT 1
#validation-timeout: 2000
#minimum-idle: ?? ??? HikariCP? ?? ???? ?? ?? ?? ?
#idle-timeout: ????? ?? ?? ??
#max-lifetime: ?? ? pool ??? connection? ?? ?? (ms)???.
#auto-commit: auto commit ?? (default true)
# hibernate? ???? SQL? ?? ???.

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace